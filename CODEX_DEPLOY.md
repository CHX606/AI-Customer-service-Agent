# 给服务器 Codex 的部署任务

请把同目录 AI Customer Service Agent 项目部署到这台服务器，执行工作，不要只给方案。

用户已确认：全新部署，不迁移原电脑上的数据库、会话、上传文件或密钥。包内 `resources/knowledge/` 和默认可乐云资料属于项目内置资源，应保留。服务器系统和内存未知，请你先检查。

先阅读 `DEPLOY.md`、`DEPLOYMENT_CHECKS.md`（如存在）、`compose.deploy.yaml` 和 `infra/deploy/`，校验 `SHA256SUMS`。主要入口是 `back.api:app`，前端使用 `/api` 同域代理。采用现成 Docker Compose 和 Caddy 配置，不要另写部署框架。

按以下顺序完成：

1. 检查 OS/CPU/AVX2、内存、磁盘（特别是 Docker 数据目录）、Docker/Compose、现有容器/网站/代理、端口和外网下载能力。本方案要求 Linux amd64；其他架构先说明适配缺口。配置不足时明确报告实际差额，不能把关闭 OCR、检索或鉴权当作部署完成。
2. 选择独立安装目录，保留已有服务。依服务器实际情况安装必要运行工具，按 `preflight.sh` 检查系统参数。不要停止无关容器、覆盖既有网站、关闭防火墙或运行全局清理命令。
3. 运行 `configure.py` 生成服务器独立密钥。检查服务器上已有的、用户授权本项目使用的模型配置；缺少 API_KEY/BASE_URL/MODEL 或域名时，只集中询问真正缺少的内容，并继续其余不依赖它们的准备。不要把密钥写进网页、Git、日志或回复。
4. 依 `DEPLOY.md` 构建镜像，准备三个本地模型，启动 OpenSearch，首次建立 default 知识索引，启动后端和网页。镜像构建必须通过 `pip check`，模型准备必须完成。遇到依赖或下载错误先定位，不要静默更换主版本。
5. 先通过回环地址检查，再接入真实域名/HTTPS。已有代理时复用它，保留 Authorization 和流式传输；管理入口为 `/manage`。保持后端鉴权开启、8000/9200/9600 不暴露公网。管理页使用 Caddy 的密码认证，再由代理注入后端管理员密钥。
6. 执行静态页、健康、匿名拒绝、管理登录、真实聊天、真实检索、图片问答、上传索引和重启持久化验收。最终核对浏览器访问。模型返回一段降级提示并不代表真实模型验收通过；启动健康也不等于全部功能可用。
7. 记录部署地址、安装目录、启动/日志/备份方法和实际镜像版本。私下交付管理凭据或告知服务器凭据文件路径；不要在最终总结直接展示模型 API 密钥或数据库密码。列出通过的检查及任何尚未通过的项目，不能把待验证项写成成功。

这是单机部署包，不包含本机模型缓存、离线依赖或预构建 Linux 镜像。模型权重需要服务器下载，正式容量、TLS 和真实模型验收由本次服务器部署完成。
